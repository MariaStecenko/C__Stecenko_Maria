#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x;
    cout << "Введіть x: ";
    cin >> x;
    double result = cos(x); 
    cout << "cos(x) = " << result << endl;
    return 0;
}
